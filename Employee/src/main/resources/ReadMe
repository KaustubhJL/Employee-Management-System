1. app (View)
	1. crudApp - main class
	2. Choices - choices for enums
	
2. checkaccess	(Controller)
	checkaccess - checks user entered password.

3. loginCredForCruds
	loginCreds - contains admin details, add and change password methods.
	
4. modelconstructor (Model)
	Employee - declares employee constructor

5. crudinterface 
	Cruds - declares the parameters for each operation.
	
6. operations
	Called from crudApp. Each calls the manager page.

7.  manager
	Manage - responsible for calling all operations from crudmethods. -in case of admin, calls the methods in loginCreds.

8. crudmethods
	runs the actual crud operations by performing ArrayList methods.

9. exceptions
	contains exceptions
 
 
 
 
 flow:
 crudApp -> operations -> manage-> crudmethods
 